require 'spec_helper'

describe Adb::Peco do
  it 'has a version number' do
    expect(Adb::Peco::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
